# forecasting

Non-LLM regime and event forecasts feeding projects 1 and 2 through the
shared ledger. Data pipelines use `shared.data.fred` and `shared.data.prices`.
Output contract: `forecasting/out/latest.json` (gitignored).
